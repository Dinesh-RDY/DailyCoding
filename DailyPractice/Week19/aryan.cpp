#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, m, k;
    cin >> n >> k;
    vector<int> ans;
    while(k--){
        int e, n1, n2;
        cin >> e >> n1 >> n2;
        if(e == 1 && n1 >= 1){
            ans.push_back(1);
        }else if(e == 2){
            
        }
    }
}